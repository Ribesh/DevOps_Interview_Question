# How to debug a live container


0. First Check if the container is running
    ```bash
    docker ps
    docker ps -a
    ```

1. Check the logs
    ```bash
    docker logs -f <container_name>
    ```

2. Go inside the container
    ```bash
    docker exec -it <container_name> bash
    ```