## Q. Why use helm in kubernetes?


### Answer:
-   Helm is basically a way in which we can manage our Kubernetes application.
    -   It is very important when we have many application running in our cluster.

    -   Helm will not only help us to deploy the application but 
    1. maintain a version of it, 
    2.  share it with others, 
    3.  rollback made easy